<div class="container">
  <div class="mt-4 p-3">
     <%= render 'shared/cardshow', auction: @auction %>
     <%= render 'shared/timer', deadline: @auction.deadline.strftime("%b %d, %Y %H:%M:%S") %>
  </div>
</div>
